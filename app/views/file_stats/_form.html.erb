<%= form_with(model: file_stat, local: true) do |form| %>
  <% if file_stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file_stat.errors.count, "error") %> prohibited this file_stat from being saved:</h2>

      <ul>
        <% file_stat.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filename %>
    <%= form.text_area :filename %>
  </div>

  <div class="field">
    <%= form.label :most_status %>
    <%= form.check_box :most_status %>
  </div>

  <div class="field">
    <%= form.label :least_status %>
    <%= form.check_box :least_status %>
  </div>

  <div class="field">
    <%= form.label :palindrome_status %>
    <%= form.check_box :palindrome_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
